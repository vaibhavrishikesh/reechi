# Copy to .env and fill in. NEVER commit the real .env (it is gitignored).
GEMINI_API_KEY=your_google_ai_studio_key_here
APP_TOKEN=dev-reechi-token
# Optional overrides:
# GEMINI_MODEL=gemini-2.5-flash-image
# PORT=8787
