# Xcode / Swift
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
.swiftpm/

# xcodegen generates this from project.yml — don't commit it
*.xcodeproj

# macOS
.DS_Store

# Secrets / local config (never commit API keys)
*.env
.env
Secrets.xcconfig

# Node (server/)
node_modules/
